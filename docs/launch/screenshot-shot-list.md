# Screenshot shot list

Use demo or scrubbed sample data only. Do not screenshot real client tasks, real names, private project names, internal URLs, magic tokens, or admin secrets.

## Screenshot 1 - Board overview

Purpose: show the main value immediately.

Frame:

- desktop width
- three to four columns visible
- sample project selected
- realistic but fake tasks
- dark theme preferred if it looks cleaner

Captured filename:

`docs/assets/screenshots/01-board-overview.png`

## Screenshot 2 - Mobile view

Purpose: prove it works on a phone.

Frame:

- phone viewport
- one active column visible
- collapsed/back navigation visible if relevant
- no private browser chrome

Captured filename:

`docs/assets/screenshots/02-mobile-board.png`

## Screenshot 3 - Task comments

Purpose: show that it is not just a static board.

Frame:

- task detail/comment panel open
- fake comment attribution
- no real user names unless deliberately generic

Captured filename:

`docs/assets/screenshots/03-task-comments.png`

## Screenshot 4 - Admin panel (skip for public launch)

Purpose: not recommended for public launch. The public demo intentionally logs in as a non-admin user, so admin screenshots are unnecessary and risk implying admin access is part of the demo.

Frame:

- admin user list or link generation UI
- no real magic token visible
- if a link is visible, use a fake/expired demo token

Suggested filename:

`docs/assets/screenshots/admin-panel.png`

## Optional GIF - drag and drop

Purpose: quick proof of interaction.

Frame:

- move a fake task from Ready to In Progress
- 4 to 6 seconds max
- small file size

Suggested filename:

`docs/assets/screenshots/drag-task.gif`
