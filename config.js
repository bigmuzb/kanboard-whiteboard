// Kanboard Whiteboard Configuration
// Fallback only. In normal Docker/server usage, /config.js is generated dynamically by server.js from environment variables.
const CONFIG = {
  API_URL: '/api',
  REFRESH_INTERVAL: 30000,
  BRAND_NAME: 'Kanboard Whiteboard',
  DEMO_MODE: false,
};
